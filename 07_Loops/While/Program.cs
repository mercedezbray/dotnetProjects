// Declare a variable
int number = 0;

// Keyword Condition
while (number <=100) //If condition is true, loop!
{
    //Loop
    // Print the number and add five
    Console.WriteLine(number);
    number = number + 5;
}

//If in an infinite loop ctrl+c to stop running